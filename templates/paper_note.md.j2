---
title: "{{ title }}"
arxiv_id: "{{ arxiv_id }}"
arxiv_version: "{{ arxiv_version or '' }}"
authors:
{% for a in authors %}
  - "{{ a }}"
{% endfor %}
categories:
{% for c in categories %}
  - "{{ c }}"
{% endfor %}
published: "{{ published }}"
updated: "{{ updated }}"
abs_url: "{{ abs_url }}"
pdf_url: "{{ pdf_url }}"
source: "gmail-arxiv"
date_folder: "{{ date_folder }}"
tags:
{% for t in tags %}
  - "{{ t }}"
{% endfor %}
---

# {{ title }}

## 一句话总结
{{ one_sentence_summary }}

## 主要内容
{% for item in main_content %}
- {{ item }}
{% endfor %}

## 关键要点
{% for item in key_points %}
- {{ item }}
{% endfor %}

## 方法
{{ method }}

## 主要结论
{{ findings }}

## 局限性
{{ limitations }}

## 链接
- [Abstract]({{ abs_url }})
- [PDF]({{ pdf_url }})